<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Test | Contrast Of Beauty]]></title>
  <link href="http://blackat.github.io/blog/categories/test/atom.xml" rel="self"/>
  <link href="http://blackat.github.io/"/>
  <updated>2014-12-16T21:07:18+01:00</updated>
  <id>http://blackat.github.io/</id>
  <author>
    <name><![CDATA[black@t]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Spring Framework Testing]]></title>
    <link href="http://blackat.github.io/blog/2013/05/08/spring-framework-testing/"/>
    <updated>2013-05-08T16:48:00+02:00</updated>
    <id>http://blackat.github.io/blog/2013/05/08/spring-framework-testing</id>
    <content type="html"><![CDATA[<p>Enterprise application are hosted in a server environement such as a servlet container or a full JEE application server.
To test the application, the developer can use only the <em>public interface</em> of the application like a web service, a messaging service, RMI or a web browser.</p>

<p>Architecture recommendations for Spring allow to produce clean layering and componentization of the codebase that will facilitate unit testing. It will be possible to test a service layer without accessing persistent data while running unit tests.</p>

<!-- more -->


<h2>Scenario</h2>

<p>The implementation of the service layer involve different changes in the data base schema and corresponding testing to check the correctness of queries and schema. Quite often during the prototyping phase is normal to change the schema of the database so an agile and light way of testing is required.</p>

<p><strong>System Under Test (SUT).</strong> It is the <em>service layer</em>.
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
</pre></td><td class='code'><pre><code class='java'><span class='line'><span class="kd">public</span> <span class="kd">class</span> <span class="nc">BulbProductServiceImplTest</span> <span class="o">{</span>
</span><span class='line'>    <span class="nd">@Autowired</span>
</span><span class='line'>    <span class="n">BulbProductService</span> <span class="n">bulbProductService</span><span class="o">;&lt;/</span><span class="n">p</span><span class="o">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">pre</span><span class="o">&gt;&lt;</span><span class="n">code</span><span class="o">&gt;</span><span class="nd">@Test</span>
</span><span class='line'><span class="kd">public</span> <span class="kt">void</span> <span class="nf">testFindAll</span><span class="o">()</span> <span class="kd">throws</span> <span class="n">Exception</span> <span class="o">{</span>
</span><span class='line'>    <span class="n">BulbProducts</span> <span class="n">bulbProducts</span> <span class="o">=</span> <span class="n">bulbProductService</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
</span><span class='line'><span class="o">}</span>
</span><span class='line'><span class="o">&lt;/</span><span class="n">code</span><span class="o">&gt;&lt;/</span><span class="n">pre</span><span class="o">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="o">&gt;}</span>
</span></code></pre></td></tr></table></div></figure></p>

<p>It would be great being able to test the method outside the application server without any external enterprise infrastructure that is <em>in isolation</em>.</p>

<p>To ensure that BulbProductServiceImpl class performs its business correctly using H2-in-memory database to host data model and testing data, JPA providers (Hibernate and Spring Data JPA&rsquo;s repository abstraction).</p>

<p>Consider the following steps:</p>

<ol>
<li><strong>Spring profiles definition.</strong> Define profiles for development, testing and production to have databases with different characteristics according to the specific environment.</li>
<li><strong>Infrastructure classes.</strong> To form a base class for service layer testing with a specific testing configuration for the database.</li>
<li><strong>Service layer testing.</strong> Implementation of the service layer tests in isolation from the application container.</li>
</ol>


<h3>Spring profiles definition</h3>

<p>Starting from Spring 3.1 M1 the new <em>bean definition profile</em> feature has been introduced to configure the container when configuration classes are used.</p>

<p><code>@Configuration</code> classes, introduced in Spring 3.1, are the pure Java equivalent to Spring <code>&lt;beans/&gt;</code> XML files. Each XML component has its counterpart class, for instance the XML component <code>&lt;jdbc:embedded-database/&gt;</code> is represented by the class <code>EmbeddedDatabaseBuilder</code>.</p>

<p>Thus it is possible to write a class which represents the <code>@Configuration</code> of the <code>ApplicationContext</code> specific to test the service layer. This way of configuring the container is <em>string-free</em> and <em>type-safe</em>.</p>

<p>Components.</p>

<ul>
<li><strong>Entity.</strong> A thing having attributes and partecipating in relationships to other entities. The OO paradigm adds behaviors to an entity calling it object.

<ul>
<li>An entity is a fine-grained business domain object which can be created, updated and deleted in any context.

<ul>
<li><em>In-memory entity.</em> Simple Java object managed by the JVM which can be easily changed without being persisted.</li>
<li><em>Database entity.</em> A transaction context is required to commit changes in the database.</li>
</ul>
</li>
<li>Metadata expressed through annotations defined in the <code>javax.persistence</code> package enables the persistence layer to recognize, interpret and properly manage the entity.</li>
</ul>
</li>
<li><strong>Entity object.</strong> In-memory instances of entity classes, the instance is a an object state which, when made persistent, represents a physical row in the database.</li>
<li><strong>Entity manager.</strong> Interface used to interact with a persistent context, each <code>EntityManager</code> manages its own persistence context.</li>
<li><strong>Persistent context.</strong> Collection of all the managed objects of an Entity Manager. If an entity object has to be retrieved but exists in the persistent context it is returned without actually accessing the database.

<ul>
<li><em>Main role.</em> A database entity object is represented by no more than one in-memory entity object within the same EntityManager.</li>
<li><em>Local cache.</em> A persistent context can be seen as a local cache for a given an <code>EntityManager</code>.</li>
</ul>
</li>
</ul>


<h3>Key points</h3>

<ul>
<li>Unit testing focus on the components of the application in <em>isolation</em>.</li>
<li>IoC principle adds a value to unit testing and Spring Framework can simplify the integration testing.</li>
<li>Spring Framework provides mock objects and testing support classes to facilitate testing in certain scenario.</li>
<li>Spring module <code>spring-test</code> is the first class support for integration testing.</li>
</ul>


<h3>Mock objects</h3>

<h3>Unit testing support classes</h3>

<h2>Integration Testing</h2>

<ul>
<li><strong>Focus on.</strong>

<ul>
<li>testing outside any container</li>
<li>testing without relying on any specific application server or deployment environment</li>
<li>avoid connection and set up of any external enterprise infrastructures</li>
<li>able to test components in isolation</li>
<li>test correct wiring and data access</li>
<li>simplify testing unit with Spring container</li>
<li>dependency injection can remove direct dependencies such as the ones on the entity manager making the unit testing difficult ?????

<ul>
<li>the advantage of the DI???</li>
</ul>
</li>
</ul>
</li>
<li><strong>In practice.</strong>

<ul>
<li><code>spring-test</code> module and <code>org.springframework.test</code> package</li>
<li><code>org.springframework.test.context</code> package, annotation driven framework to provide unit and integration testing support

<ul>
<li>agnostic of the actual testing framework in use allowing instrumentation of JUnit, TestNG, etc</li>
</ul>
</li>
</ul>
</li>
<li><strong>Annotations.</strong>

<ul>
<li><code>@ContextConfiguration</code> to configure an <code>ApplicationContext</code> for the test.

<ul>
<li>Dependencies of the test instances are <em>injected</em> from beans defined in the application context.</li>
<li>If no locations are specified, the configured <code>ContextLoader</code> loads a context from a default set of location.

<ul>
<li><code>GenericXmlContextLoader</code> is the default context loader and generates the file name and the location based on the name of the test class adding the suffix <code>-context.xml</code>.</li>
</ul>
</li>
<li>Provide an array of <em>resource locations</em> of XML configuration metadata such as the one provided in the <code>web.xml</code> deployment descriptor.</li>
<li><code>ApplicationContext</code> is reused for each test so the setup cost happens only once per test fixture.</li>
</ul>
</li>
<li><code>@DirtiesContext</code> can be used to annotate a method causing the rebuild of the application context before executing the next test.</li>
</ul>
</li>
</ul>


<p>Testing outside the server facilitates the way to get an enterprise application bug free but also reduces the time and makes the unit testing more complete and automatic.</p>

<p>The advantage is to test without deployment and any connection to an external enterprise infrastructure allowing the developer to test
  * correct wiring of the Spring IoC container contexts,
  * data access using JDBC or an ORM tool being able to also test SQL statements, queries, JPA entity mappings and strategies and so on.</p>

<p>Spring support for integration testing is expressed by <code>spring-test</code> module which encapsulate the  <code>org.springframework.test</code> package, a set of classes useful to perform integration testing with a Spring container.</p>

<h3>Spring TestContext Framework</h3>

<p>Framework located in the <code>org.springframework.test.context</code> package put a lot of importance on <em>convention over configuration</em>.</p>

<ul>
<li><strong>Core of the framework.</strong>

<ul>
<li><code>TestContext</code> class encapsulates the context in which a test is executed, agnostic of the testing framework in use.</li>
<li><code>TestContextManager</code> class is the main entry point of the framework, manages a <em>single</em> <code>TestContext</code> and signals events to all registered <code>TestExecutionListener</code>.</li>
<li><code>TestExecutionListener</code> is an listener interface that, if implemented, let to react to test execution events published by <code>TestContextManager</code>. The listener API is made of well-define exectuion point methods to be implemented in order to provide specific behavior during test execution.

<ul>
<li><code>DependencyInjectionTestExecutionListener</code>, <code>DirtiesContextTestExecutionListener</code>, and <code>TransactionalTestExecutionListener</code>.</li>
</ul>
</li>
</ul>
</li>
<li><strong>Dependency injection.</strong>

<ul>
<li>Dependencies of the test instances are injected from beans in the application context configured through <code>@ContextConfiguration</code>.</li>
</ul>
</li>
<li><strong>JUnit 4.5+ support classes.</strong>

<ul>
<li><code>org.springframework.test.context.junit4</code> package</li>
<li><code>AbstractJUnit4SpringContextTests</code> integrates the context framework with <code>ApplicationContext</code> testing support in a JUnit environment.</li>
<li><code>AbstractTransactionalJUnit4SpringContextTests</code> transactional extension which expects in the application context

<ul>
<li><code>javax.sql.DataSource</code> bean</li>
<li><code>PlatformTransactionManager</code> bean</li>
</ul>
</li>
<li><code>@RunWith(SpringJUnit4ClassRunner.class)</code> allows developer to implement standard JUnit 4.5+ unit and integration tests and get the benefits of the TestContext framework such as dependency injection of test instances, transactional test method execution, etc.

<ul>
<li>Transaction management will roll back the transaction after the execution of each <em>test method</em>.</li>
</ul>
</li>
</ul>
</li>
</ul>


<p>Notes</p>

<ul>
<li><code>@Configuration</code> classes are pure-java configuration, the equivalent of the Spring XML metadata files.</li>
</ul>

]]></content>
  </entry>
  
</feed>
